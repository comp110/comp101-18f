import { print } from "introcs";

class Pizza {
    size: string = "small";
    toppings: number = 0;
}

export let main = async () => {

    // Given a pizza order:
    // - large, 3-toppings
    // - small, 2-toppings
    
    // Print a description and price per pizza for any size order.
    // Print the total price of any size order.
    
};

let price = (pizza: Pizza): number => {
    // The price of a Pizza is
    // $7 for small, $11 for large
    // $0.75 per topping
    return 0;
};

main();